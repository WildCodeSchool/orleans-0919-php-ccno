{% extends 'layout.html.twig' %}
{% block content%}
    <div class="container">
        <div class="row">
            <p>{{ detail.datetime }}</p>
            <p>{{ detail.name }}</p>
        </div>


    </div>
{% endblock %}

